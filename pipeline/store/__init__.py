"""
pipeline.store — storage backends for the pipeline.

  clickhouse.py — insert Records into ClickHouse `records` table
  postgres.py   — log pipeline run metadata to PostgreSQL `pipeline_runs`
"""
