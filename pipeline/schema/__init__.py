"""
pipeline.schema — shared data contracts for the pipeline.

  record.py     — Record dataclass: the universal time-series row
  validators.py — pydantic v2 models for source-specific raw API responses
"""
